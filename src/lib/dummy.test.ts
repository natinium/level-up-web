// Simple dummy test to ensure CI passes when no real tests exist yet
import { describe, it, expect } from "vitest";

describe("dummy", () => {
  it("should pass", () => {
    expect(true).toBe(true);
  });
});
